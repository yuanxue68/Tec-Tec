$('#notifications').append("<%= j render(@notifications) %>");
<% if @notifications.next_page %>
  $('pagination').replaceWith('<%=j will_paginate @notifications, renderer: BootstrapPagination::Rails %>');
<% else %>
  $('.pagination').remove();
<% end %>
